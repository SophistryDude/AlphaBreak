# Status Reconciliation

**Created**: March 15, 2026
**Purpose**: Documents features listed as planned or in-progress in the README/Roadmap that were verified as already complete on the live EC2 instance.

This document was generated by directly inspecting the live server (`3.140.78.15`), database, and running processes.

---

## Summary

| Item | Documented Status | Actual Status |
|------|-------------------|---------------|
| SSL/HTTPS | ⚠️ Planned Q2 2026 | ✅ Complete |
| Frontend served by Nginx | Not documented (said python http.server port 8000) | ✅ Complete |
| Airflow operational | `[ ]` Incomplete in README roadmap | ✅ Complete, running since Feb 9 |
| Dark pool data | Not in roadmap | ✅ 101 MB dataset, 621K rows |
| CBOE options stats | Not in roadmap | ✅ 3,253 rows in production |
| 13F archive tables | Not documented | ✅ 1.27 GB, 8.4M rows |
| Dashboard tab count | README says 4 tabs | ✅ 9 tabs live |
| Portfolio tab | Not in README | ✅ portfolio.js live on server |
| Forex pairs | README says 12 pairs | ✅ 21 pairs, 123K rows |

---

## Detail

### 1. SSL/HTTPS

**Roadmap said**: `⚠️ No SSL/HTTPS (planned Q2)` under Current Status; listed as a Q2 April 2026 task.

**Reality**: SSL is fully configured and operational via Let's Encrypt / Certbot.
- HTTPS on port 443 with valid certificates
- HTTP (port 80) returns 301 redirect to HTTPS
- `www.alphabreak.vip` redirects to `alphabreak.vip`
- Certificate managed by Certbot with auto-renewal

**Evidence**: `/etc/nginx/sites-enabled/alphabreak.vip` contains:
```nginx
listen 443 ssl;
ssl_certificate /etc/letsencrypt/live/alphabreak.vip/fullchain.pem;
ssl_certificate_key /etc/letsencrypt/live/alphabreak.vip/privkey.pem;
include /etc/letsencrypt/options-ssl-nginx.conf;
```

---

### 2. Frontend Served by Nginx (Not Python http.server)

**Documented**: PROJECT_CONTEXT.md and SETUP_GUIDE.md stated frontend was served on port 8000 via `python -m http.server`.

**Reality**: Frontend is served directly by Nginx from `/home/ubuntu/frontend/` on port 443 (HTTPS). The Python http.server is no longer running. Nginx also proxies `/api/` to Flask on port 5000.

**Impact**: Deployment commands in PROJECT_CONTEXT.md that reference port 8000 and python http.server are stale. The "sync frontend to flask_app/frontend" step is also unnecessary — only `/home/ubuntu/frontend/` is served.

---

### 3. Airflow Fully Operational

**README Roadmap said**: `[ ] Kubernetes migration with Airflow` — implying Airflow was not yet deployed.

**Reality**: Airflow has been running since **February 9, 2026** with full LocalExecutor setup:
- `airflow scheduler` (pid 4090349, running since Feb 9)
- `airflow webserver --port 8080` (active)
- `airflow executor -- LocalExecutor` + 30+ worker processes
- 2 active DAGs: `portfolio_update_dag.py`, `portfolio_sync_onetime.py`
- 29 DAG runs recorded, 280 task instances executed

The README roadmap item was referring to K8s-based Airflow (not yet done), but bare-metal Airflow has been fully operational for over a month.

---

### 4. Dark Pool Data Pipeline

**Roadmap**: Not mentioned in any quarterly plan. Listed only as a "Future Consideration" under "Advanced Features."

**Reality**: Fully populated in production:
- `darkpool_weekly_volume`: 621,387 rows, 101 MB — weekly dark pool volume by ticker
- `darkpool_ticker_aggregates`: 22,940 rows, 4 MB — rolling aggregates per ticker

This is the 2nd largest dataset in the database by size, yet has no documentation and no corresponding frontend feature or API endpoint documented.

---

### 5. CBOE Options Statistics

**Roadmap**: Not mentioned.

**Reality**: `cboe_market_options_stats` has 3,253 rows (656 kB) of CBOE daily market-level options data, and `cboe_options_volume` exists in the schema. No API endpoint or frontend feature documented for this data.

---

### 6. 13F Archive Tables

**Documented**: README and DATA_ARCHITECTURE listed 5 13F tables: `hedge_fund_managers`, `f13_filings`, `f13_holdings`, `f13_stock_aggregates`, `cusip_ticker_map`.

**Reality**: 4 additional archive tables exist and are heavily populated:

| Table | Rows | Size |
|-------|------|------|
| `f13_archive_holdings` | 8,384,814 | 1,268 MB |
| `f13_archive_aggregates` | 29,854 | 6.1 MB |
| `f13_archive_stocks` | 41,242 | 4.2 MB |
| `f13_archive_institutions` | 2,222 | 248 kB |

The archive holdings table alone is **1.27 GB** and represents the largest dataset in the entire database, covering the full SEC EDGAR 13F filing history across all tracked institutions.

---

### 7. Dashboard Tab Count

**README said**: "Web dashboard (4 tabs)"

**Reality**: 9 JavaScript frontend files are live on the server, each corresponding to a tab:
`app.js`, `auth.js`, `dashboard.js`, `descriptions.js`, `earnings.js`, `forex.js`, `indicators.js`, `longterm.js`, `portfolio.js`, `reports.js`, `trading.js`, `watchlist.js`

That's at minimum 9 feature tabs (Sentiment/Dashboard, Trading, Options/Reports, Watchlist, Earnings, Long-term, Indicators, Forex, Portfolio).

---

### 8. Portfolio Tab

**README**: Not mentioned in the frontend file list or tab list.

**Reality**: `portfolio.js` is live on the server. Portfolio tables have data (`portfolio_holdings` has 11 open positions, `portfolio_signals` has 135 signals).

---

### 9. Forex Pairs Count

**Roadmap current status said**: "Forex correlation analysis (12 pairs, 50+ years data)"
**README said**: "21 currency pairs tracked"

**Reality**: `forex_daily_data` has 123,068 rows across 21 pairs. The "12 pairs" figure in the Roadmap current status section was stale at time of writing.

---

## Undocumented Tables Requiring Future Documentation

These tables exist in production with data but have no corresponding documentation, API endpoints, or frontend features:

| Table | Rows | Notes |
|-------|------|-------|
| `darkpool_weekly_volume` | 621,387 | Significant dataset, no docs |
| `darkpool_ticker_aggregates` | 22,940 | No docs |
| `cboe_market_options_stats` | 3,253 | No docs |
| `forex_equity_correlations` | — | Schema exists, purpose unclear |
| `forex_models` | — | Model config table, not documented |
| `market_breadth_intraday` | — | Schema exists, pipeline unclear |
| `engineered_features` | — | ML feature matrix, not in current docs |
| `trend_break_features` | — | Not documented |
| `trend_break_predictions` | — | Not documented |
| `trend_ranges` | — | Not documented |
| `backtest_results` | — | Not documented |
| `indicator_accuracy` | — | Not documented |
| `model_performance` | — | Not documented |

---

## Recommended Follow-Up

1. **Dark pool**: Either add a frontend tab + API endpoints, or document the data pipeline and its intended use
2. **PROJECT_CONTEXT.md**: Update deployment commands to reflect Nginx serving (no python http.server, no port 8000)
3. **README**: Update tab count from 4 to 9, add portfolio.js to file list, correct forex pairs to 21
4. **Airflow**: Clarify the README roadmap item — K8s Airflow is still pending, bare-metal Airflow is complete
