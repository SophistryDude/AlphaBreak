# Flask API Dependencies
# =======================

# Web Framework
Flask>=2.3.0
Flask-CORS>=4.0.0
Flask-Limiter>=3.5.0
Flask-Caching>=2.1.0

# WSGI Server
gunicorn>=21.0.0

# WebSocket / Real-time — install separately in a dedicated SocketIO worker.
# Why commented out: eventlet monkey-patches stdlib networking and breaks the
# default gunicorn sync workers + psycopg2 connection pool used by the REST API.
# How to enable: build a second image that installs these two packages and runs
# `gunicorn -k eventlet -w 1 wsgi:app` on a separate port, leaving the REST API
# on its existing sync-worker gunicorn.
# flask-socketio>=5.3.0
# eventlet>=0.35.0

# Data Processing
pandas>=2.0.0,<2.2.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0

# Technical Analysis - using ta library as alternative to pandas_ta
ta>=0.11.0

# Market Data
yfinance>=0.2.28
lxml>=4.9.0

# Machine Learning Models
xgboost>=2.0.0
lightgbm>=4.0.0
scikit-learn>=1.3.0

# Database (optional)
psycopg2-binary>=2.9.0
SQLAlchemy>=2.0.0

# Redis for rate limiting
redis>=5.0.0

# Authentication
PyJWT>=2.8.0
bcrypt>=4.1.0

# NLP / Sentiment
vaderSentiment>=3.3.2

# Logging & Monitoring
python-json-logger>=2.0.0
# Prometheus metrics — install separately if needed.
# Why commented out: the package pulls a Flask pin that conflicts with our
# Flask>=2.3 baseline and blocks `pip install -r requirements.txt`.
# How to enable: `pip install prometheus-flask-instrumentator --no-deps` after
# the rest of requirements.txt is installed; the app already import-guards it
# in flask_app/app/__init__.py so metrics light up automatically.
# prometheus-flask-instrumentator>=4.0.0

# AWS SES (email delivery for password reset, smart alerts, portfolio events)
boto3>=1.34.0

# Utilities
python-dotenv>=1.0.0
